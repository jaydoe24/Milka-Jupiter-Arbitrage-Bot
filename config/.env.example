# ═══════════════════════════════════════════════════════
#  Milka Jupiter Arbitrage Bot — Configuration
#  Copy to: config/.env  and fill in your values
# ═══════════════════════════════════════════════════════

# ── NETWORK ─────────────────────────────────────────────
# "devnet" for testing, "mainnet" for real trading
NETWORK=devnet

# ── RPC (Helius) ─────────────────────────────────────────
# Get your key at: https://dashboard.helius.dev
# Devnet:  https://devnet.helius-rpc.com/?api-key=YOUR_KEY
# Mainnet: https://mainnet.helius-rpc.com/?api-key=YOUR_KEY
RPC_URL=https://devnet.helius-rpc.com/?api-key=YOUR_HELIUS_API_KEY
WS_URL=wss://devnet.helius-rpc.com/?api-key=YOUR_HELIUS_API_KEY

# ── WALLET ──────────────────────────────────────────────
# ⚠️  Always use a burner wallet — NEVER your main wallet!
PRIVATE_KEY=your_base58_private_key_here

# ── TRADING ─────────────────────────────────────────────
TRADE_AMOUNT=0.02            # SOL per trade — start small!
MIN_PROFIT_PERCENT=0.5       # Minimum net profit % to execute
MAX_PRICE_IMPACT=1.0         # Max price impact % per swap leg
SLIPPAGE_BPS=50              # Slippage tolerance (50 = 0.5%)

# ── HELIUS SENDER ────────────────────────────────────────
# Sender broadcasts to validators + Jito simultaneously for max speed
# Tip accounts and min tip (0.0002 SOL) are handled automatically
# Choose the region closest to your VPS:
#   ewr = Newark (US-East)   ← best for most Solana validators
#   slc = Salt Lake City
#   lon = London
#   fra = Frankfurt
#   ams = Amsterdam
#   sg  = Singapore
#   tyo = Tokyo
SENDER_REGION=ewr

# ── LOGGING ─────────────────────────────────────────────
LOG_LEVEL=info               # debug | info | warn | error

# ── TELEGRAM ALERTS (optional but strongly recommended) ──
# Setup: Message @BotFather → /newbot → copy token
#        Message @userinfobot → copy your chat ID
# Leave as "disabled" to skip
TELEGRAM_BOT_TOKEN=disabled
TELEGRAM_CHAT_ID=
